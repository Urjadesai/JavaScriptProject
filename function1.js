// ------------------ Que-1-B---------------

// we can pass parameters as many as we want according to requirement.

function display(para1, para2, para3, para4)
{
    var result = (para1, para2, para3, para4) * 4;
    document.write(result);
}

// ------------------ Que-1-C---------------

function display1(x,y,z)
{
    var ans = x * y * z;
    document.write(ans);
    alert(ans);
}